# AWS ECS Environment Configuration
# Used for AWS ECS/Fargate deployment

# Backend API URL - Update with your AWS Application Load Balancer URL
# Example: http://your-backend-alb-1234567890.us-east-1.elb.amazonaws.com
VITE_BACKEND_URL=http://your-backend-alb.region.elb.amazonaws.com

# OpenSearch Configuration
# Update with your AWS OpenSearch Service endpoint
# Example: https://search-radar-abcd1234.us-east-1.es.amazonaws.com
OPENSEARCH_HOST=your-opensearch-endpoint.region.es.amazonaws.com
OPENSEARCH_PORT=443
OPENSEARCH_PROTOCOL=https
OPENSEARCH_USERNAME=admin
OPENSEARCH_PASSWORD=your-secure-password

# OpenSearch Indices
OPENSEARCH_INDEX_SCANS=radar-scans
OPENSEARCH_INDEX_DEVICES=radar-devices

# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=production

# AWS Specific
AWS_REGION=us-east-1
