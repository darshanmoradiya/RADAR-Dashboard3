# Backend Environment Configuration
# Copy this file to .env and update with your values

# OpenSearch Configuration
OPENSEARCH_HOST=192.168.92.143
OPENSEARCH_PORT=9200
OPENSEARCH_PROTOCOL=https
OPENSEARCH_USERNAME=admin
OPENSEARCH_PASSWORD="MyStrong#Pass90"

# OpenSearch Dashboard
OPENSEARCH_DASHBOARD_URL=https://192.168.92.143:5601

# Indices
OPENSEARCH_INDEX_SCANS=radar-scans
OPENSEARCH_INDEX_DEVICES=radar-devices

# Server Configuration
PORT=3001
HOST=0.0.0.0

# Polling Configuration (milliseconds)
POLL_INTERVAL=30000
CACHE_TTL=10000

# Production Settings
NODE_ENV=production
LOG_LEVEL=info

# For Docker deployments, use service names:
# OPENSEARCH_HOST=opensearch
# HOST=0.0.0.0

# For AWS deployments, use the OpenSearch endpoint:
# OPENSEARCH_HOST=search-your-domain.region.es.amazonaws.com
# OPENSEARCH_PROTOCOL=https
# OPENSEARCH_PORT=443
